# Simplified production cutover — 2026-07-31

## Outcome

The fresh three-workflow pipeline was cut over in `Asia/Manila` and observed through real schedule boundaries. Exactly one Scraper, one Evaluator & Generator, and one Alerter & Mover are active. No workflow submits an application.

The production workbook is `1WDzgk83Js6QemgTA0-_Xemipfy_J7PdNf_Mdyedoing`. It contains the visible `Review Queue`, `Applied Jobs`, and `Archive` tabs plus hidden `_System`. Setup ran twice without duplicate tabs, headers, validation, protections, or records. No old rows were imported.

The retained old workbook is `1ORq6ImOOJ1a0ZLoH8a2PlKHWX5jmQyBu4fRlGQWFkRE`; its Drive backup is `1dwb3G_ANorUOcfNyY8K60JdfjUJWw00pQ4YUI3hg6pE`. Its observed update timestamp stayed at `2026-07-30T19:02:21.869Z` throughout replacement execution, and no active replacement binds it.

## Backups and inventory

- Encrypted pre-cutover n8n export: `/Users/johnlesterescarlan/.codex/backups/job-pipeline/20260731T021435PHT/n8n-production-pre-simplified-cutover.tar.gz.enc`.
- The encrypted backup contains 62 unique exported workflow records.
- The complete post-cutover instance inventory contains 13 unique workflow IDs: 3 active replacements, 7 retained inactive legacy roles, and 3 inactive disposable smoke workflows. Records present only in the pre-cutover export remain recoverable from the encrypted backup.
- Active Scraper: `qxPbOzNs5StaPY8B`.
- Active Evaluator & Generator: `TRUqD9atneyDyMNx`.
- Active Alerter & Mover: `QO6OLK3pHetgGIGq`.
- The managed LaunchAgent reads the production workbook, rollback workbook, Review Queue URL, Groq key, and Slack webhook from macOS Keychain. The live listener passes deployment policy `2026-07-31/v1`.

## Non-production smoke

The disposable workbook started blank and passed a second idempotent setup run. Smoke coverage included:

- fixed inclusive rolling 24-hour discovery, old/future/missing timestamp exclusion, and duplicate suppression;
- ready, review-needed, approval, denial, automatic skip, user skip, `I Applied`, Applied Jobs movement, Archive movement, and outcome recording;
- source, provider, stale-action, destination-write, source-delete, Slack rejection, Slack timeout, repeated-run, and recovery paths;
- one authorized Slack delivery whose code block matched the stored generated message, with open-only Review Queue and source links;
- no automatic application submission and no Apply Points use.

## Scheduled production observations

- Alerter & Mover completed repeated empty-queue runs without alerts or moves.
- The `04:08` Scraper run succeeded and inserted 6 listings. All 6 had matched keywords, unique canonical IDs, and posting ages from 2.06 to 19.85 hours relative to the fixed run window.
- The `04:32` Generator run succeeded and processed exactly one record. It routed that record to `skip`, cleared its processing stage/token, and left 5 records `new`.
- The original `04:34` Alerter offset exposed a two-minute Generator overlap and reached its 120-second timeout. No destination or alert side effect was committed.
- Alerter & Mover was shifted from offset 4 to offset 14, producing `14/29/44/59` minute starts. Runtime validation now rejects worst-case Generator/Alerter overlap.
- The `04:44` Alerter & Mover run succeeded, moved the one `skip` record to Archive with `automatic_skip`, and pruned expired discovery claims.
- The `04:59` run succeeded and pruned the remaining expired movement lease.

Final workbook state:

- Review Queue: 5 `new`;
- Applied Jobs: 0;
- Archive: 1 `skip` / `automatic_skip`;
- total canonical IDs: 6;
- duplicate canonical IDs, out-of-window jobs, processing residue, alert IDs, and `_System` claim rows: 0.

## Validation

- `npm run validate`: 172 tests, 160 passing, 12 intentional retired-legacy skips, 0 failures.
- Production-context deployment validation passed against the managed listener.
- Sanitized `post_activation` evidence passed `npm run validate:cutover`.
- Post evidence contains 13 workflows, exactly 3 active replacements, all required observation flags, and zero webhook/API-key/authorization pattern hits.
- GitGuardian passed on draft PR #46.

No webhook URL, API key, authorization header, full private resume, job description, or generated application payload is included in this report.
